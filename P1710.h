#include<algorithm>
#include<vector>

using namespace std;


class Solution_p1710
{
public:
	int maximumUnits(vector<vector<int>>& boxTypes, int truckSize);
	bool static cmp(vector<int> a, vector<int> b);
};